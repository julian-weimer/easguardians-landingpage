import colors from "../common/colors.js";

const AnimatedRandomGlyphs = () => {
  return (
    <div
      css={{ height: "12px", background: colors["blue-500"], width: "12rem" }}
    />
  );
};

export default AnimatedRandomGlyphs;
